# Auto-HTTPS (Let's Encrypt) for $DOMAIN, proxying to the game server.
# Caddy passes WebSocket + geckos signaling through transparently.
# Host networking → reach the app on localhost (no docker DNS).
{$DOMAIN} {
	reverse_proxy 127.0.0.1:2567
}
